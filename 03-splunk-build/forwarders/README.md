# Universal Forwarder Configurations

This folder keeps repository-safe forwarder configuration for systems that have actually been onboarded.

Current host:

- [`dns-soc-web01/inputs.conf`](dns-soc-web01/inputs.conf) — required Nginx monitor stanzas only
- [`dns-soc-web01/outputs.conf`](dns-soc-web01/outputs.conf) — private destination `10.50.20.10:9997`

No credentials, certificates, authentication databases or private keys are stored here.
